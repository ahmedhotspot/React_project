import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    alignItems: 'center',
    justifyContent: 'center',
    width: '100%',
    alignSelf: 'center',
  },
  logo: {
    marginBottom: 0,
  },
  fallbackText: {
    fontSize: 24,
    fontWeight: 'bold',
    color: '#e74c3c',
  },
});

